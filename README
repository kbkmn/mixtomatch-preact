# Project Installation

To install the project dependencies, follow these steps:

1. Make sure you have pnpm installed globally. If not, you can install it by running the following command: `npm install -g pnpm`

2. Navigate to the project directory in your terminal.

3. Run the following command to install the project dependencies: `pnpm install`

4. Once the installation is complete, you can start using the project.

## Firebase

Copy .env.local.example to .env.local and fill the data.

## Commands

This project uses `pnpm` for managing scripts. Here are some common commands you might need:

`pnpm dev`
To start the development server, run the following command:

`pnpn build`
To build project to dist directory, run the following command:

`pnpm deploy`
To build and deploy to firebase server, run the following command:
